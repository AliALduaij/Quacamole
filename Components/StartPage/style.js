import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    fontSize: 50
  },

  content: {
    top: 300,
    flex: 1,

    alignItems: "center"
  },

  text: {
    fontSize: 20,
    textAlign: "center"
  }
});

export default styles;
